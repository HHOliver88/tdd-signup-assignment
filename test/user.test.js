const chai              = require('chai');
const UserController    = require('../controllers/user.controller');

describe('Sign up', function(){
    it('The /signup route should render the signup view.');
    /* add your pending tests here */
});